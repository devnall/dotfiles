function dbuild() {
  docker build . -t $1 --build-arg GITHUB_TOKEN=$GITHUB_TOKEN  --build-arg SIDEKIQPRO_AUTH=$SIDEKIQPRO_AUTH
}

# TODO: Delete this line || it's a quick test of github's search capabilities
