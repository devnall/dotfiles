# .ackrc file

# Always sort files
--sort-files

# Always color, even if piping to another program
--color

# Group matches by filename
--group

# Smart case - case sensitive if upper/mixed case, insensitive if all lower (use --no-smart-case to force sensitive) 
--smart-case

# Colors
--color-filename=bright_green
--color-lineno=bright_blue
--color-match=yellow

# Set tf type to search Terraform but not tfstate files
--type-set=terraform:ext:tf

# Other types to set
--type-add=vim=.vim,.snippets
--type-set=md=.markdown,.md,.mkd,.txt

# Ignore some stuff
--ignore-dir=node_modules
